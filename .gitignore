# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Turborepo
.turbo

# TanStack Router generated route tree
apps/web/src/routeTree.gen.ts

# TypeScript incremental build cache
*.tsbuildinfo

# Cloudflare / Wrangler
**/.wrangler/
**/.dev.vars
# NB: worker-configuration.d.ts is committed — regenerate via `pnpm cf-typegen`
# after editing wrangler.jsonc (it's derived but pinned so fresh clones build).

# Better Auth CLI throwaway SQLite (used only by `auth:generate` to pick the SQL flavor)
**/.auth-cli.sqlite

# Eval harness — local secrets and cached results
apps/evals/.env
apps/evals/.promptfoo/
apps/evals/**/.promptfoo/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
