{
  "name": "sufra-evals",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Evaluation harness for Sufra's vision model selection. Custom EstimatorProvider routes promptfoo through the production estimator (worker/meals/estimator/) — same code path as the Worker.",
  "engines": {
    "node": ">=22.22.0"
  },
  "scripts": {
    "eval": "NODE_OPTIONS='--import tsx' PROMPTFOO_DISABLE_TELEMETRY=1 promptfoo eval --no-cache --no-table --output /tmp/promptfoo-results.json -c promptfooconfig.ts",
    "eval:view": "PROMPTFOO_DISABLE_TELEMETRY=1 promptfoo view",
    "report": "tsx report.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openrouter/ai-sdk-provider": "^2.0.0",
    "ai": "^6.0.0",
    "dotenv": "^17.0.0",
    "openai": "^6.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "promptfoo": "^0.121.11",
    "tsx": "^4.20.0",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
